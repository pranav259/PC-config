monitor=eDP-1,1920x1080@144,auto,1


$taskManager = plasma-systemmonitor
$fileSysytem = qdirstat
$terminal = ghostty
$calculator = qalculate-gtk
$whatsapp = elecwhat
$fileManager = thunar
$menu = rofi -show drun
$browser = zen-browser
$spotify = spotify

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:



exec-once= mako
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = hyprsunset
exec-once = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark" 
exec-once = gsettings set org.gnome.desktop.interface gtk-theme "Sweet"
exec-once = hyprpaper
exec-once = waybar
exec-once = blueman-applet
exec-once = nm-applet --indicator
exec-once = wl-paste -t text --watch clipman store --no-persist
exec-once = dunst
#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XCURSOR_SIZE,20
env = HYPRCURSOR_SIZE,2
env = KDE_FULL_SESSION,1
env = QT_QPA_PLATFORM, wayland
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_QPA_PLATFORMTHEME,qt6ct
env = GTK_THEME, Adwaita:dark
env = GTK_ICON_THEME, Sweet-Dark
env GDK_BACKEND=x11 davinci-resolve

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in =5 
    gaps_out =10

    border_size = 3

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(800080ee) 
    col.inactive_border = rgba(000000aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 5
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.8 

    shadow {
        enabled = true
        range = 4
        render_power = 9
        color = rgba(0,255,0,0.5)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 4
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:escape
    kb_rules =

    follow_mouse = 1

    scroll_factor = 2 

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -1
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = CTRL SHIFT, escape, exec, $taskManager
bind = ALT, escape, exec, $fileSysytem
bind = $mainMod, F, exec, thunderbird
bind = $mainMod, 70, exec, discord
bind = , 148, exec, $calculator
bind = $mainMod, T, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, F1, exec, $browser
bind = $mainMod, F2, exec, $whatsapp
bind = ,179, exec, $spotify
bind = $mainMod, F3, exec, psensor
bind = ALT, SPACE, exec, $menu
bind = , 225, exec, $menu
bind = $mainMod, Y, togglefloating,
bind = ALT, V, exec, for i in {1..6}; do dunstctl history-pop; done



bind = $mainMod, P, pseudo,
bind = $mainMod SHIFT, J, togglesplit, 
bind = $mainMod SHIFT, K, togglesplit,
bind = $mainMod, M, fullscreen, 1
bind = , F11, fullscreen
bind = ALT, Tab, cyclenext  
bind = ALT, Tab, bringactivetotop  
bind = ALT, W, killactive,

bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d



bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod, 20, workspace, 11
bind = $mainMod, 21, workspace, 12


bind = SUPER SHIFT, H, swapnext
bind = SUPER SHIFT, L, swapnext

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 10, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = $mainMod SHIFT, 20, movetoworkspace, 11
bind = $mainMod SHIFT, 21, movetoworkspace, 12

bind = $mainMod, Left ,workspace, m-1
bind = $mainMod, Right ,workspace, m+1

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = ALT SHIFT, F4, exec, poweroff
bind = CTRL $mainMod, F4, exec, reboot
binde = $mainMod SHIFT, H, moveactive,-50 
binde = $mainMod SHIFT, L, moveactive, 50 0
# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = ALT CTRL, mouse:272, movewindow
bindm = ALT CTRL, mouse:273, resizewindow

bind = $mainMod, N, togglespecialworkspace, magic
bind = $mainMod, N, movetoworkspace, +0
bind = $mainMod, N, togglespecialworkspace, magic
bind = $mainMod, N, movetoworkspace, special:magic
bind = $mainMod, N, togglespecialworkspace, magic

bind = ,Print, exec, hyprshot -m window
bind = ALT , Print, exec, hyprshot -m output
bind = SHIFT,Print, exec, hyprshot -m region


bind = SUPER CTRL , L, resizeactive, 45 0
bind = SUPER CTRL , H, resizeactive, -45 0
bind = SUPER CTRL , K, resizeactive, 0 -45
bind = SUPER CTRL , J, resizeactive, 0 45
# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl  set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-
bind = $mainMod,Down, exec, brightnessctl set 5%-
bind = $mainMod,Up, exec, brightnessctl set 5%+

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play
bindl = , XF86AudioPlay, exec, playerctl play
bindl = , XF86AudioPrev, exec, playerctl previous
bindl = ,174, exec, playerctl stop


bind = CTRL ALT, 1,exec, powerprofilesctl set power-saver
bind = CTRL ALT, 2,exec, powerprofilesctl set balanced
bind = CTRL ALT, 3,exec, powerprofilesctl set performance


bind = SUPER, V, exec, clipman pick -t wofi
bind = ALT SHIFT, F5, exec, systemctl suspend

bind = SHIFT, XF86MonBrightnessUp, exec, ~/.local/bin/adjust_temp.sh up
bind = $mainMod SHIFT, Up, exec, ~/.local/bin/adjust_temp.sh up
bind = SHIFT, XF86MonBrightnessDown, exec, ~/.local/bin/adjust_temp.sh down
bind = $mainMod SHIFT, Down, exec, ~/.local/bin/adjust_temp.sh down



##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland

windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
