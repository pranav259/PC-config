#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias c='clear'
alias n='nvim'
alias vim='nvim'
alias e='exit'
alias wifi='nmtui'
alias w='nmtui'
PS1='[\u@\h \W]\$ '
